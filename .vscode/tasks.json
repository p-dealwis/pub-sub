{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile Main",
            "type": "shell",
            "command": "g++ -std=c++11 -o main main.cpp Tag.cpp hash.cpp `libgcrypt-config --libs`",
            "args": [
                "-lssl",
                "-lcrypto",
                "-lsodium"
            ],
            "problemMatcher": []
        }
    ]
}