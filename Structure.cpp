using namespace std;

#include "Structure.h"

Structure::Structure(Tag* contained,Tag* left,Tag* right){
    _contained = contained;
    _left = left;
    _right = right
}

Structure::Structure(Tag* contained){
    _contained = contained;
}

Structure::makeStructure(Tag []){

}